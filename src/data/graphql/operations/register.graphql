# 用户注册
mutation Register($input: RegisterInput!) {
  register(input: $input) {
    token
    user {
      id
      name
      nickname
      avatar
      gender
      bio
      phone
      email
      is_guest
    }
  }
}
